# Academic-Dropout-Detection
A binary classification project for academic dropout detection (Dropout vs Non-Dropout) using Random Forest and XGBoost, including preprocessing, EDA, ROC/PR curves, confusion matrices, and threshold analysis.
